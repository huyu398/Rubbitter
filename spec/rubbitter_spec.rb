require 'spec_helper'

describe Rubbitter do
  it 'has a version number' do
    expect(Rubbitter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
